{
  "name": "@masochistme/root",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "api:dev": "lerna run api:dev:watch",
    "api:build": "lerna run api:build",
    "sdk:build": "lerna run sdk:build",
    "build": "yarn sdk:build && yarn api:build",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}
